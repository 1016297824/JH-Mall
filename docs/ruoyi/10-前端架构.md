# 前端架构

## 技术栈
- Vue 2 + Element UI（管理端 `ruoyi-ui`）
- Vuex 状态管理
- Vue Router（history 模式）
- Axios HTTP 请求
- SCSS 样式预处理器

## 目录结构
```
ruoyi-ui/src/
├── api/           # API 接口定义
├── assets/        # 静态资源
├── components/    # 公共组件
├── directives/    # 自定义指令
├── layout/        # 布局组件
├── plugins/       # 插件
├── router/        # 路由配置
├── store/         # Vuex 状态管理
├── utils/         # 工具函数
└── views/         # 页面视图
```

## Vuex Store 模块

| 模块 | 说明 |
|------|------|
| `user.js` | 用户信息（token、角色、权限） |
| `permission.js` | 路由权限管理 |
| `dict.js` | 字典数据缓存 |
| `app.js` | 应用状态（侧边栏、设备类型） |
| `tagsView.js` | 标签页导航 |
| `settings.js` | 系统设置 |
| `lock.js` | 锁屏状态 |

## 路由架构

### 静态路由（constantRoutes）
始终可用，无需登录：
- `/login`、`/register`、`/404`、`/401`、`/lock`
- `/redirect/:path(.*)` 重定向
- `/` 首页（仪表盘）
- `/user/profile` 个人中心

### 动态路由（dynamicRoutes）
需要特定权限才能访问：
- `/system/user-auth/role/:userId` — 用户分配角色
- `/system/role-auth/user/:roleId` — 角色分配用户
- `/system/dict-data/index/:dictId` — 字典数据
- `/monitor/job-log/index/:jobId` — 调度日志
- `/tool/gen-edit/index/:tableId` — 代码生成编辑

### 动态路由加载流程
1. 用户登录 → 调用 `/menu/getRouters` 获取路由树
2. `permission.js` 递归转换路由组件
3. `router.addRoutes()` 注入动态路由
4. `filterDynamicRoutes()` 按权限过滤

## API 封装

### 请求拦截
- 自动添加 `Authorization` Header
- Token 刷新机制（401 时自动刷新）
- 统一错误处理

### 响应拦截
- 统一解析响应数据
- 错误码处理
- 401 跳转登录页

## 自定义指令

| 指令 | 说明 |
|------|------|
| `v-hasPermi` | 权限按钮控制 |
| `v-hasRole` | 角色按钮控制 |
| `v-copy` | 复制文本 |

## 全局组件

| 组件 | 说明 |
|------|------|
| `TreePanel` | 树形侧边栏（部门选择） |
| `ExcelImportDialog` | Excel 导入弹窗 |
| `RightToolbar` | 表格工具栏 |
| `ParentView` | 多级路由父容器 |
| `InnerLink` | 内嵌外部页面 |

## 字典混入
全局混入 `dict` 功能，组件中声明 `dicts` 数组自动加载字典数据：
```javascript
export default {
  dicts: ['sys_normal_disable', 'sys_user_sex'],
  created() {
    // dict.type.sys_normal_disable 可直接使用
  }
}
```
