# 商品详情页 UX 检查报告 #09

> 检查日期：2026-06-17 | 检查对象：`ProductDetailPage.vue` + 4 个子组件

## 总览

| 维度 | 得分 | #08 → #09 变化 |
|:----:|:----:|:--------------|
| 设计一致性 | **9/10** | 9（不变，发现 3 处硬编码色值 P2） |
| 交互完整性 | **9/10** | 9（不变） |
| 可访问性 | **9/10** | 7 → 9（P0/P1 清零） |

---

## 1. 设计一致性（对照 `docs/ui-design/00_MASTER.md`）

### 1.1 通过项

| 检查项 | 规范 | 实际 | 结果 |
|--------|------|------|:----:|
| 主色 Primary | `#0369A1` | `v.$color-primary` | ✅ |
| CTA 色 | `#22C55E` → `#16A34A` hover | `.action__btn--cart` | ✅ |
| H1 字号（含 Mobile） | `28px / 700` | `.product-name` 28px / 移动端不再降级 | ✅ |
| Price 字号 | `20px / 700 / Rubik` | `.price-current` 匹配 | ✅ |
| Body 描述 | `14px / 400 / 1.5` | `.product-desc` 14px | ✅ |
| 间距系统 | 8px 基准 | 全部用 `$spacing-*` | ✅ |
| 卡片圆角 | `12px` (radius-lg) | `.price-box` 12px | ✅ |
| 按钮圆角 | `8px` (radius-md) | CTA 按钮 8px | ✅ |
| 阴影 | sm → md → lg | 卡片 sm、hover md | ✅ |
| 动效时长 | 150/200/300ms | `$duration-fast/normal/slow` | ✅ |
| 过渡曲线 | `cubic-bezier(0.4,0,0.2,1)` | `$ease-default` | ✅ |
| 按钮 active | `scale(0.97)` | 全部统一 | ✅ |
| 骨架屏色值 | 设计系统色板 | `$color-bg-card` + `$color-border` | ✅ |
| 字体 | Rubik + Nunito Sans | `$font-heading` / `$font-body` | ✅ |

### 1.2 发现项

| # | 严重度 | 问题 | 文件 | 位置 |
|:--:|:------:|------|------|:----:|
| 1 | 🟢 P2 | `.price-box` 渐变硬编码 `#f8fbfe` / `#eff6fb`，应改用 `$color-bg` 或 `$color-bg-card` | `ProductInfoSection.vue` | L141 |
| 2 | 🟢 P2 | `.qty-btn { background: #f5f8fa; }` 硬编码，不在设计系统 | `ProductInfoSection.vue` | L222 |
| 3 | 🟢 P2 | `.price-original { color: #7a94a6; }` 硬编码，应定义为设计 Token | `ProductInfoSection.vue` | L170 |

---

## 2. 交互完整性（对照 `docs/design/15_前端详细设计-C端.md`）

### 2.1 5 步 SKU 交互流程全部通过

| 步骤 | 设计要求 | 实现 | 结果 |
|:----:|---------|------|:----:|
| 1 | 加载 SPU → 构建规格矩阵 | `loadDetail()` → `deriveSpecGroups(attrs)` | ✅ |
| 2 | 点选规格 → 匹配 SKU → 显示价格+库存 | `onSpecSelect` → `currentSku` → `currentPrice`/`stock` | ✅ |
| 3 | 未选完整 → 按钮灰色 | `canBuy` computed | ✅ |
| 4 | 已选 → 数量选择器 | `quantity` + `maxQuantity` + `输入框` | ✅ |
| 5 | 加入购物车 / 立即购买 | `addToCart()` + `buyNow()` | ✅ |

### 2.2 边界状态

| 状态 | 处理 | 结果 |
|------|------|:----:|
| 加载中 | 骨架屏（色值已对齐设计系统） | ✅ |
| 加载失败 | 错误提示 + 重试按钮 | ✅ |
| 无 SKU 属性 | 规格区隐藏 | ✅ |
| SKU 详情异步加载 | `skuLoading` + "查询库存中..." | ✅ |
| 售罄 | 图片叠蒙层 + 按钮 disabled | ✅ |
| 登录校验（购买） | 检查 `accessToken`，跳转 `/login?redirect=` | ✅ |
| 加入购物车失败 | `ElMessage.error` 提示 | ✅ |

---

## 3. 可访问性检查（WCAG 2.1 AA）

### 3.1 通过项

| # | 检查项 | 实现 | 结果 |
|:--:|--------|------|:----:|
| 1 | 颜色对比度 ≥ 4.5:1 | 正文 `#0C4A6E` on `#F0F9FF` ≈ 13:1 | ✅ |
| 2 | 焦点可见 | 全局 `:focus-visible { outline: 2px solid #0369A1 }` | ✅ |
| 3 | 键盘可操作 | 按钮、SKU 选择器、Tabs 均可 Tab | ✅ |
| 4 | 表单错误 | 加入购物车失败有 Toast | ✅ |
| 5 | 图片 alt | 主图 `"商品名 主图"`，缩略图 `"缩略图 N"` | ✅ |
| 6 | 语义化 HTML | `<h1>` + `<section>` + `<dl>` + `<main>` | ✅ |
| 7 | ARIA 标签 | Tab: `role=tablist/tab/tabpanel` + `aria-selected` + `aria-controls` | ✅ |
| 8 | `prefers-reduced-motion` | `global.scss` 全局规则 | ✅ |
| 9 | 表单标签关联 | `<label for="qty-input">` + `<input id="qty-input">` | ✅ |
| 10 | Skip Link | `跳到主要内容` | ✅ |
| 11 | Tab 焦点管理 | `switchTab()` → `nextTick()` → `panel.focus()` | ✅ |
| 12 | ARIA live | `.stock-info` 有 `aria-live="polite"` | ✅ |
| 13 | 装饰符 aria-hidden | `.promise-check` ✓ 有 `aria-hidden="true"` | ✅ |
| 14 | disabled Skipped | SKU 禁用按钮 `tabindex="-1"` | ✅ |

### 3.2 发现项

无新发现。P0/P1 已全部清零。

---

## 4. 历次报告得分对比

| 维度 | #05 | #06 | #07 | #08 | #09 |
|:----:|:---:|:---:|:---:|:---:|:---:|
| 设计一致性 | 6 | 9 | 10 | 9 | **9** |
| 交互完整性 | 7 | 7 | 9 | 9 | **9** |
| 可访问性 | 3 | 6 | 9 | 7 | **9** |
| P0 数量 | 7 | 0 | 0 | 1 | **0** |
| P1 数量 | 4 | 4 | 0 | 4 | **0** |
| P2 数量 | — | — | — | 3 | **3** |
| **综合得分** | **5.3** | **7.3** | **9.3** | **8.3** | **9.0** |

---

## 5. 总结

**P0 清零、P1 清零。** 综合得分回到 9.0（与 #07 持平）。

3 个 P2 均为 `ProductInfoSection.vue` 中的硬编码色值（渐变背景 `#f8fbfe`/`#eff6fb`、数量按钮 `#f5f8fa`、划线价 `#7a94a6`），可择机统一到 `variables.scss`。
