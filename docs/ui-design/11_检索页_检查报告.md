# 检索页 UX 检查报告 #11

> 检查日期：2026-06-20 | 检查对象：`SearchPage.vue` | #10 修复后复查

## 总览

| 维度 | #10 → #11 | 变化 |
|:----:|:---------:|:--------------|
| 设计一致性 | 7/10 → **9/10** | CSS 变量全部对齐 Token；硬编码清零 |
| 交互完整性 | 6/10 → **8/10** | 6 处错误提示 + 高亮渲染 + 排序滚动复位 |
| 可访问性 | 4/10 → **7/10** | 搜索/筛选/结果区 ARIA 补齐 + aria-busy |

---

## 1. 设计一致性（对照 `docs/ui-design/00_MASTER.md`）

### 1.1 通过项

| 检查项 | 规范 | 实际 | 结果 |
|--------|------|------|:----:|
| 主色 Primary | `#0369A1` | `v.$color-primary` | ✅ |
| CTA 色 | `#22C55E` → `#16A34A` | `v.$color-cta` | ✅ |
| 字体 Heading | Rubik | `v.$font-heading` | ✅ |
| 字体 Body | Nunito Sans | 默认继承 Element Plus | ✅ |
| 间距系统 | 8px 基准 | `v.$spacing-*` Token 全量 | ✅ |
| CSS 变量 | 禁用 `var(--el-text-*)` | 7 处全部替换为 `v.$color-text-*` | ✅ |
| 图标色值 | 用 Token | `colorBorder` ref 动态绑定 | ✅ |
| 搜索框居中 | 搜索驱动型首页布局 | `margin: 0 auto` | ✅ |
| 商品名称 | H3 = 18px/600 | `font-size: 18px; font-weight: 600` | ✅ |
| 商品价格 | Price = 20px/700/Rubik | `20px/700/v.$font-heading` | ✅ |
| 卡片阴影 | hover → md | `shadow="hover"` | ✅ |
| 边框 | `#BAE6FD` | `v.$color-border` | ✅ |
| 圆角 | `4px`(sm)/`8px`(md) | `v.$radius-sm`/`v.$radius-md` | ✅ |
| 动效 | 150/200/300ms | `v.$duration-*` + `v.$ease-default` | ✅ |
| 图片 lazy | `loading="lazy"` | 已设置 | ✅ |

### 1.2 发现项

| # | 严重度 | 问题 | 位置 |
|:--:|:------:|------|:----:|
| 1 | 🟢 P2 | `colorBorder = '#BAE6FD'` 硬编码在 script 里（SCSS 变量无法在 `<script>` 直接引用，暂无更优方案） | L23 |

---

## 2. 交互完整性（对照 `docs/design/15_前端详细设计-C端.md`）

### 2.1 通过项

| 步骤 | 设计要求 | 实现 | 结果 |
|:----:|---------|------|:----:|
| URL 同步 | 从 query 读取 keyword | `route.query.keyword` + `onMounted` | ✅ |
| 全文搜索 | POST `/api/search` | `store.doSearch()` + `.catch(ElMessage.error)` | ✅ |
| 搜索建议 | GET `/api/search/suggest` | `store.fetchSuggestions()` + 200ms 防抖 | ✅ |
| 聚合筛选 | 类目/品牌/价格区间 | 完整 | ✅ |
| 排序切换 | 5 种排序 | `SORT_OPTIONS` + `el-radio-group` | ✅ |
| 排序后滚回顶部 | 用户应回到结果顶部 | `window.scrollTo({ top: 0 })` | ✅ |
| 分页加载 | 加载更多 | `store.loadMore()` + `.catch` | ✅ |
| 空结果 | 提示 + 建议 | 按钮文案改善 | ✅ |
| 加载中 | 骨架屏 | `el-skeleton` × 6 | ✅ |
| 错误提示 | Toast | `ElMessage.error` × 6 | ✅ |
| 高亮渲染 | `<em>` 标签 | `v-html="spuNameHighlight \|\| spuName"` | ✅ |
| 响应式 | PC/Tablet/Mobile | 筛选侧栏 → 抽屉 | ✅ |

### 2.2 发现项

| # | 严重度 | 问题 | 位置 |
|:--:|:------:|------|:----:|
| 2 | 🟡 P1 | 移动端筛选抽屉内的类目/品牌列表无 ARIA（与 PC 端不对称） | L430-457 |

---

## 3. 可访问性（WCAG 2.1 AA）

### 3.1 通过项（#10 → #11 修复）

| # | 检查项 | 实现 | 结果 |
|:--:|--------|------|:----:|
| 1 | 搜索区 ARIA | `role="search"` + `aria-label="商品搜索"` | ✅ |
| 2 | 筛选列表 ARIA | `role="listbox"` + `role="option"` + `aria-selected` | ✅ |
| 3 | 结果区 ARIA | `role="region"` + `aria-label` + `aria-live="polite"` | ✅ |
| 4 | 排序组 ARIA | `aria-label="排序方式"` | ✅ |
| 5 | 骨架屏 ARIA | `aria-busy="true"` | ✅ |
| 6 | 图片 alt | `:alt="item.spuName"` | ✅ |
| 7 | `prefers-reduced-motion` | 全局规则 | ✅ |
| 8 | 语义化 `<h3>` | 卡片标题 | ✅ |
| 9 | loading="lazy" | 图片延迟加载 | ✅ |
| 10 | 对比度 | `#0C4A6E` on `#F0F9FF` ≈ 13:1 | ✅ |

### 3.2 发现项

| # | 严重度 | 问题 | 位置 |
|:--:|:------:|------|:----:|
| 2 | 🟡 P1 | 移动端筛选抽屉列表缺 ARIA（PC 端已有） | L430-457 |
| 3 | 🟡 P1 | 搜索建议弹窗键盘导航：上下箭头无法在建议项间移动，ESC 未确认 | L206-215 |
| 4 | 🟢 P2 | 价格输入框无 `inputmode="numeric"` | L285, L293 |
| 5 | 🟢 P2 | 加载更多后焦点丢失 | L411 |

---

## 4. 得分对比

| 维度 | #10 | #11 |
|:----:|:---:|:---:|
| 设计一致性 | 7/10 | **9/10** |
| 交互完整性 | 6/10 | **8/10** |
| 可访问性 | 4/10 | **7/10** |
| P0 数量 | 6 | **0** |
| P1 数量 | 6 | **2** |
| P2 数量 | 3 | **3** |
| **综合得分** | **5.7** | **8.0** |

---

## 5. 按优先级汇总

### P0 — 0 项 ✅

P0 全部清零。

### P1 — 2 项

| # | 类别 | 问题 | 修复方向 |
|:--:|------|------|---------|
| 2 | 可访问性 | 移动端筛选抽屉列表缺 ARIA | 加 `role="listbox"` + `aria-selected` |
| 3 | 可访问性 | 建议列表键盘导航不完整 | 箭头键移动焦点 + ESC 关闭 |

### P2 — 3 项

| # | 类别 | 问题 | 修复方向 |
|:--:|------|------|---------|
| 1 | 设计 | `colorBorder` 硬编码在 script 中 | 无法直接用 SCSS；可改为 CSS Variable |
| 4 | 可访问性 | 价格输入框缺 `inputmode` | 加 `inputmode="numeric"` |
| 5 | 可访问性 | 加载更多后焦点丢失 | `nextTick` + `focus()` |

---

## 6. 总结

P0 从 6 项清零。综合得分从 5.7 升至 **8.0**。剩余 2 个 P1（移动端筛选 ARIA 不对称、建议键盘导航）、3 个 P2。
