# 检索页 UX 检查报告 #12

> 检查日期：2026-06-20 | 检查对象：`SearchPage.vue` | #11 修复后复查

## 总览

| 维度 | #11 → #12 | 变化 |
|:----:|:---------:|------|
| 设计一致性 | 9/10 → **9/10** | 不变 |
| 交互完整性 | 8/10 → **9/10** | 聚合实时刷新 + 筛选持久化修复 |
| 可访问性 | 7/10 → **8/10** | 复选框视觉提示 + 移动端 ARIA 对齐 |

---

## 1. 设计一致性（对照 `docs/ui-design/00_MASTER.md`）

### 1.1 通过项

| 检查项 | 规范 | 实际 | 结果 |
|--------|------|------|:----:|
| 主色 Primary | `#0369A1` | `v.$color-primary` | ✅ |
| CTA 色 | `#22C55E` | `v.$color-cta` | ✅ |
| 字体 Heading | Rubik | `v.$font-heading` | ✅ |
| 间距系统 | 8px 基准 | `v.$spacing-*` 全量 | ✅ |
| CSS 变量 | 禁用 `var(--el-text-*)` | 全部替换 | ✅ |
| 搜索框居中 | `margin: 0 auto` | ✅ | ✅ |
| 商品名称 | H3 = 18px/600 | ✅ | ✅ |
| 价格 | 20px/700/Rubik | ✅ | ✅ |
| 卡片阴影/边框/圆角 | 设计 Token | 全部对齐 | ✅ |
| 动效 | 150/200ms | `v.$duration-*` | ✅ |
| 复选框 | Element Plus 默认 | `el-checkbox size="small"` | ✅ |
| `__filter-label` | 8px 间距 | `margin-left: 8px`（硬编码，可优化） | ⚠️ |

### 1.2 发现项

| # | 严重度 | 问题 | 位置 |
|:--:|:------:|------|:----:|
| 1 | 🟢 P2 | `colorBorder = '#BAE6FD'` 硬编码（同 #11） | L23 |
| 2 | 🟢 P2 | `__filter-label` 的 `margin-left: 8px` 硬编码，应改用 `v.$spacing-xs`(4px) 或 `v.$spacing-sm`(6px) | L720 |

---

## 2. 交互完整性（对照 `docs/design/15_前端详细设计-C端.md`）

### 2.1 通过项

| 功能 | 状态 |
|------|:----:|
| URL 关键词同步 | ✅ |
| 全文搜索 + 错误提示 | ✅ |
| 搜索建议（防抖 + 建议点击） | ✅ |
| 多选类目/品牌筛选 + 复选框 | ✅ |
| 价格区间筛选（元→分自动转换） | ✅ |
| 5 种排序 + 滚回顶部 | ✅ |
| 分页加载更多 + 错误提示 | ✅ |
| 空结果 + 骨架屏 | ✅ |
| 聚合实时刷新（筛选后计数更新） | ✅ |
| 已选筛选项不丢失（count=0 兜底） | ✅ |
| 从首页导航自动清空旧筛选 | ✅ |
| 响应式 PC/Tablet/Mobile | ✅ |

### 2.2 发现项

无。

---

## 3. 可访问性（WCAG 2.1 AA）

### 3.1 通过项

| # | 检查项 | 结果 |
|:--:|--------|:----:|
| 1 | 搜索区 `role="search"` + `aria-label` | ✅ |
| 2 | 筛选列表 `role="listbox"` + `role="option"` + `aria-selected`（PC + 移动端） | ✅ |
| 3 | 结果区 `role="region"` + `aria-live="polite"` | ✅ |
| 4 | 排序组 `aria-label="排序方式"` | ✅ |
| 5 | 骨架屏 `aria-busy="true"` | ✅ |
| 6 | 建议列表键盘导航（↑↓ Enter Esc） | ✅ |
| 7 | 图片 `alt` + `loading="lazy"` | ✅ |
| 8 | `prefers-reduced-motion` 全局 | ✅ |
| 9 | 价格输入 `inputmode="numeric"` | ✅ |
| 10 | 对比度 ≥ 4.5:1 | ✅ |

### 3.2 发现项

无。

---

## 4. 得分对比

| 维度 | #10 | #11 | #12 |
|:----:|:---:|:---:|:---:|
| 设计一致性 | 7 | 9 | **9** |
| 交互完整性 | 6 | 8 | **9** |
| 可访问性 | 4 | 7 | **8** |
| P0 | 6 | 0 | **0** |
| P1 | 6 | 2 | **0** |
| P2 | 3 | 3 | **2** |
| **综合得分** | 5.7 | 8.0 | **8.7** |

---

## 5. 按优先级汇总

### P0 / P1 — 0 项 ✅

全部清零。

### P2 — 2 项

| # | 类别 | 问题 | 修复方向 |
|:--:|------|------|---------|
| 1 | 设计 | `colorBorder = '#BAE6FD'` 在 script 中硬编码 | 改为 CSS Variable |
| 2 | 设计 | `__filter-label` 的 `margin-left: 8px` 硬编码 | 改用 `v.$spacing-*` Token |

---

## 6. 总结

综合得分 **8.7/10**。P0/P1 全部清零，交互流程完整无缺陷，可访问性覆盖全面。仅剩 2 个 P2 硬编码色值优化，不影响功能交付。
